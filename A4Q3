#include <stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int a[1000];
    for(int i=0;i<n;i++) scanf("%d",&a[i]);
    int half=n/2;
    for(int i=0;i<half;i++){
        printf("%d ",a[i]);
        printf("%d",a[i+half]);
        if(i<half-1) printf(" ");
    }
    return 0;
}
